ROOT = ../../..

include $(ROOT)/common.mk

# Context test requires yasm and is macOS-specific (-f macho64)
# Skip on Linux for now
test::
	@echo "[test] Skipping context test (requires yasm + macOS)"
